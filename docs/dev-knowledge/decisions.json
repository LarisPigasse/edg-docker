{
  "lastUpdated": "2026-09-10",
  "decisions": [
    {
      "id": "ADR001",
      "date": "2025-10-01",
      "title": "Architettura microservizi con API Gateway unico punto di accesso",
      "context": "Necessità di separare i bounded context (auth, log, spedizioni) mantenendo sicurezza e scalabilità.",
      "decision": "Tutti i servizi esposti solo internamente (expose, non ports). Solo l'API Gateway ha ports verso l'esterno. Tutto il traffico passa per il gateway.",
      "rationale": "Centralizza sicurezza, CORS, rate limiting e logging. Permette scaling indipendente dei servizi.",
      "status": "accepted"
    },
    {
      "id": "ADR002",
      "date": "2025-10-15",
      "title": "Dual API Gateway per High Availability",
      "context": "Il gateway è single point of failure. Un crash blocca tutta l'applicazione.",
      "decision": "Due istanze API Gateway dietro Traefik load balancer. Rate limiting 1024 req/16min per istanza.",
      "rationale": "Elimina SPOF sul gateway mantenendo semplicità (Docker Compose, no Kubernetes).",
      "status": "accepted"
    },
    {
      "id": "ADR003",
      "date": "2025-10-20",
      "title": "RBAC con sistema permessi espliciti e negazione con prefisso !",
      "context": "Necessità di controllo accessi granulare per operatori con permessi diversi per modulo.",
      "decision": "Permission system con notazione module.action (es: spedizioni.read). Negazione esplicita con ! prefix (es: !spedizioni.delete). Deny First policy.",
      "rationale": "Segue standard AWS IAM/Kubernetes RBAC. Flessibile, estendibile, auto-documentante.",
      "status": "accepted"
    },
    {
      "id": "ADR004",
      "date": "2025-11-01",
      "title": "Redux solo per Auth e UI settings, local state per tutti gli altri moduli",
      "context": "Tentazione di usare Redux per tutti i moduli causa complessità senza benefici reali.",
      "decision": "Redux gestisce: auth (globale), UI settings (darkMode, sidebar, footer). Tutto il resto usa local state nei custom hooks.",
      "rationale": "Riduce boilerplate, ogni modulo è autonomo, più semplice da testare e mantenere.",
      "status": "accepted"
    },
    {
      "id": "ADR005",
      "date": "2026-01-10",
      "title": "email-service nel repo principale edg-docker invece di repo separato",
      "context": "email-service è infrastruttura condivisa usata da auth-service e potenzialmente altri servizi.",
      "decision": "email-service vive in edg-docker/ come gli altri microservizi, non in repository separato.",
      "rationale": "Semplifica deployment (un solo docker-compose), evita dependency management tra repo, facilita aggiornamenti coordinati.",
      "status": "accepted"
    },
    {
      "id": "ADR006",
      "date": "2026-02-01",
      "title": "accountType come VARCHAR(50) invece di ENUM",
      "context": "accountType inizialmente definito come ENUM causava problemi di migrazione e rigidità nell'aggiunta di nuovi valori.",
      "decision": "accountType è VARCHAR(50) con default 'indefinito'. Valori: operatore, partner, cliente, agente, indefinito.",
      "rationale": "ENUM richiedono ALTER TABLE per nuovi valori (problematico in produzione). VARCHAR più flessibile con validazione a livello applicativo.",
      "status": "accepted"
    },
    {
      "id": "ADR007",
      "date": "2026-02-05",
      "title": "Sistema tema light/dark con CSS custom properties + Tailwind mapping",
      "context": "Necessità di tema coerente su 52+ componenti senza duplicazione di codice colore.",
      "decision": "Due livelli: CSS vars in globals.css (:root light / .dark dark), mappate in classi Tailwind custom in index.css (@theme). Toggle via classe .dark su document.documentElement.",
      "rationale": "Un solo punto di definizione colori. Classi semantiche (bg-bg-primary, text-text-secondary) anziché hardcoded. Tailwind gestisce il cambio tema automaticamente.",
      "status": "accepted"
    },
    {
      "id": "ADR008",
      "date": "2026-03-16",
      "title": "Dev knowledge store in docs/dev-knowledge/ versionato con il codice",
      "context": "Problema di amnesia contestuale tra sessioni: errori ripetuti, stato moduli sconosciuto, decisioni dimenticate.",
      "decision": "Tre file JSON (modules.json, lessons.json, decisions.json) in edg-docker/docs/dev-knowledge/. Gestiti via tool MCP get-dev-briefing, update-module-status, log-lesson-learned, log-architecture-decision.",
      "rationale": "La conoscenza del progetto vive nel repo del progetto, è versionata con Git, accessibile programmaticamente dal Control Tower MCP.",
      "status": "accepted"
    },
    {
      "id": "ADR009",
      "date": "2026-03-25",
      "title": "Feature toggling multi-tenant: moduli attivabili per cliente",
      "context": "L'app EDG verrà distribuita a clienti diversi. Ogni cliente deve vedere solo i moduli che ha acquistato (es. uno vuole vehicles, un altro no). Serve un unico frontend configurabile per tenant.",
      "decision": "Implementare multi-tenancy con feature toggling a tre livelli: (1) Entità Tenant in auth-service con modules[], tenantId su Account, modules nel JWT al login. (2) Middleware moduleGuard nel gateway che mappa route→modulo e verifica modules dal JWT prima di forwardare. (3) Frontend usa modules dal JWT solo per UX (menu/route visibili), mai come controllo di sicurezza. La sicurezza reale è garantita esclusivamente dal gateway.",
      "rationale": "JWT firmato impedisce manomissioni client-side. Il gateway centralizza il controllo su tutti i microservizi senza duplicare logica. Il frontend resta un unico codebase. Root può configurare i moduli per tenant dalla sezione accounts. Approccio JWT scelto su chiamata API dedicata per evitare round-trip aggiuntivo all'avvio, accettando che i cambiamenti richiedano re-login.",
      "status": "accepted"
    },
    {
      "id": "ADR010",
      "date": "2026-08-26",
      "title": "Due soli frontend: app-frontend per gli utenti, pro-frontend per gli operatori",
      "context": "Vigilo (gestione scadenze, manutenzioni e controlli periodici) nasce per un'utenza più ampia dei soli clienti EDG: trasportatori, ma anche aziende agricole e di movimento terra. L'ipotesi iniziale era un frontend dedicato (asset-frontend) affiancato al modulo replicato in pro-frontend per la gestione operatori. Quell'impostazione avrebbe richiesto un frontend per ogni prodotto futuro, moltiplicando le copie del design system e imponendo un SSO cross-dominio a ogni salto fra applicazioni. Verifica sul campo: core/, features/auth e config/ esistono già in copia in pro-frontend, app-frontend e asset-frontend.",
      "decision": "Il confine fra le applicazioni è il ruolo dell'utente, non il prodotto. Due soli frontend: app-frontend, che contiene tutte le feature destinate agli utenti finali (Vigilo, logistica, spedizioni...) con menu e homepage derivati dai moduli attivi secondo ADR009; pro-frontend, riservato agli operatori EDG per la gestione di moduli, clienti e account. Vigilo è un modulo di app-frontend (features/vigilo), non un'applicazione: asset-frontend viene assorbito. Regola vincolante: i moduli non si importano fra loro — ogni features/<modulo> dipende dal core e dai pacchetti condivisi, mai da un altro modulo; ciò che serve a due moduli sale nei pacchetti condivisi.",
      "rationale": "Il confine usare/amministrare non si moltiplica al crescere dei prodotti, quello per prodotto sì: cinque prodotti significherebbero cinque frontend, cinque copie del design system e quattro salti di dominio da orchestrare. La scelta elimina inoltre alla radice il problema dell'SSO cross-dominio: senza salti fra applicazioni non servono né cookie sul dominio padre (che estenderebbero il perimetro di fiducia a ogni sottodominio, incluse le sessioni degli operatori interni) né token di handoff. Il lazy loading già presente in App.tsx evita che l'utente scarichi i moduli che non usa. La regola di non-importazione fra moduli mantiene app-frontend scomponibile e un modulo estraibile in futuro senza traumi.",
      "status": "accepted"
    },
    {
      "id": "ADR011",
      "date": "2026-08-26",
      "title": "Monorepo con pacchetti condivisi per design system e autenticazione",
      "context": "Design system (core/), autenticazione (features/auth) e configurazione (config/) esistono oggi in copia in pro-frontend, app-frontend e asset-frontend. Le correzioni applicate a una copia non raggiungono le altre, e il fenomeno è già documentabile: il Dockerfile di produzione privo di COPY --from=frontend-builder /app/dist (nginx serve una root vuota) e gli asset del tema referenziati come /src/assets/*.png (path valido solo con il dev server, 404 in build) sono stati corretti in una sola copia e restano attivi nelle altre.",
      "decision": "Monorepo unico con npm workspaces: apps/ contiene le applicazioni (pro-frontend, app-frontend), packages/ il codice condiviso — @edg/ui per il design system, @edg/auth per login, PrivateRoute, slice e apiFetch, più un pacchetto di contratti per ciascun dominio applicativo (tipi e client API). Le applicazioni importano dai pacchetti anziché possederne una copia locale. Nessun registry privato: dipendenze locali nella stessa repository. Build, deploy e domini restano separati per applicazione; i microservizi backend restano dove sono.",
      "rationale": "Una correzione scritta una volta raggiunge tutte le applicazioni, che è la proprietà oggi assente. Se una modifica al codice condiviso rompe un'applicazione, il suo build fallisce subito: con le copie lo stesso problema resterebbe silenzioso per mesi. Gli npm workspaces non richiedono infrastruttura aggiuntiva né pubblicazione di pacchetti. Il costo accettato è che il codice condiviso va toccato con più attenzione perché impatta tutti: molto minore di quello, ricorrente e dimenticabile, di ricordare quali copie aggiornare.",
      "status": "accepted"
    },
    {
      "id": "ADR012",
      "date": "2026-08-26",
      "title": "Branding per dominio: stesso build, identità di prodotto diversa",
      "context": "Vigilo si rivolge anche ad aziende agricole e di movimento terra, per le quali Express Delivery Group è un fornitore di spedizioni: mostrare quel marchio è commercialmente dissonante. Serve un'identità di prodotto autonoma senza reintrodurre una seconda applicazione, che riporterebbe la duplicazione esclusa da ADR010.",
      "decision": "app-frontend viene servito su più domini (es. app.expressdeliverygroup.com e vigilo.it) a partire dallo stesso artefatto di build. APP_CONFIG diventa funzione del dominio di accesso: nome, claim, logo, palette, favicon, titolo della pagina e insieme dei moduli proposti. Ogni cliente entra da un solo dominio e vi resta per l'intera sessione.",
      "rationale": "Ottiene l'identità di prodotto senza una seconda base di codice: il punto di variazione esiste già ed è unico, perché APP_CONFIG centralizza per costruzione nome, brand e tema. Poiché ogni utente resta sul proprio dominio non nascono salti fra applicazioni, quindi nessuna sessione da propagare fra domini e nessun meccanismo di SSO da introdurre. Il costo è limitato alla parametrizzazione della configurazione e al deploy dello stesso artefatto con variabili diverse.",
      "status": "accepted"
    },
    {
      "id": "ADR013",
      "date": "2026-08-26",
      "title": "Token semantici per azioni, superfici e testo nel design system",
      "context": "ADR007 stabilisce un solo punto di definizione dei colori, ma il tema copriva soltanto i contenitori (bg-*, text-*, border-*, --card-*, --modal-*): mancavano i colori delle azioni semantiche. Ogni componente se li inventava con utility Tailwind letterali — 139 occorrenze in 27 file, con `danger` reso come red-600 nel Button e red-500 altrove. Badge risolveva il tema con varianti `dark:` esplicite, un secondo meccanismo parallelo a quello dei token. Cambiare il brand significava aprire venti file.",
      "decision": "Tre famiglie di token in globals.css, definite per entrambi i temi e mappate su utility Tailwind in index.css: (1) `--action-{primary|secondary|neutral|danger|success|warning|info}` con `-hover` e `-text` per le superfici piene (pulsanti, avatar, barre); (2) `--surface-{sem}` con `-text` e `-border` per le superfici tenui (badge, alert, evidenziazioni); (3) `--text-{danger|success|warning}` con `-hover` per il testo semantico. Nei componenti nessun colore letterale: zero occorrenze residue. Le varianti `dark:` sui colori spariscono, perché il token cambia già col tema.",
      "status": "accepted",
      "rationale": "Testo e superficie hanno esigenze di contrasto opposte fra i due temi — un testo colorato deve scurire sul chiaro e schiarire sullo scuro, un fondo pieno no — quindi servono famiglie separate e non un unico set di colori. I valori sono scelti con contrasto testo/fondo verificato ≥ 4.5:1 (WCAG AA), cosa che i colori letterali precedenti non garantivano: `Button variant=\"warning\"` usava `text-dark`, una classe inesistente, lasciando il testo senza colore su fondo giallo. Con i token il cambio di brand torna a essere una modifica in un solo file, come ADR007 prevedeva."
    },
    {
      "id": "ADR014",
      "date": "2026-09-07",
      "title": "Convenzione di naming per tabelle Postgres: tabelle primitive, id_<entità> e uuid_<entità> separati",
      "context": "Nel disegnare le prime tabelle di system-service (operatori, reparti, anagrafiche partner/clienti/agenti) è emersa la necessità di uno standard rigoroso e ripetibile per tutte le tabelle Postgres dei servizi EDG, da applicare sempre in futuro, non solo qui.",
      "decision": "Per ogni tabella Postgres di un microservizio EDG: (1) nomi tabella e campo in italiano, snake_case — restano in inglese solo i campi standard created_at/updated_at/is_active; (2) PK intera autoincrementante chiamata id_<nome_tabella_singolare>, mai \"id\" generico; (3) identificativo pubblico separato uuid_<nome_tabella_singolare>, tipo UUID nativo Postgres, generato lato DB con DEFAULT gen_random_uuid() (mai passato come defaultValue lato Sequelize, per lasciare il DB come unica fonte); (4) ogni concetto che è in realtà un elenco chiuso/gestibile (es. reparto) diventa una tabella primitiva dedicata, referenziata con una FK intera id_<tabella_primitiva> — mai un varchar libero; (5) un CHECK enum si usa solo se i valori ammessi non potranno mai essere modificati o ampliati in futuro (es. anagrafiche.tipo: partner/cliente/agente); (6) le tabelle elementari/di lookup (es. reparti) non hanno created_at/updated_at, perché non serve storicizzarle e non vengono quasi mai modificate.",
      "rationale": "Le tabelle primitive individuate a monte evitano varchar liberi che poi si scoprono essere in realtà elenchi da amministrare (visto già con \"reparto\"), e la separazione tra PK interna e uuid pubblico permette di avere chiavi primarie leggere/efficienti per i join interni mantenendo comunque un identificativo stabile e non enumerabile da esporre nelle API e nei riferimenti cross-service (es. accounts.entityId in auth-service). Deciso con l'utente confrontando un file di esempio (tabelle_iniziali.docx) e iterando su 4 punti aperti: criterio enum-vs-tabella-primitiva, tipo del campo uuid, lingua dei nomi, timestamp sulle tabelle elementari.",
      "status": "accepted"
    },
    {
      "id": "ADR015",
      "date": "2026-09-07",
      "title": "Icone Lucide: catalogo unico in icons.ts + iconMap solo per concetti trasversali",
      "context": "Ogni componente frontend importava le icone che gli servivano direttamente da 'lucide-react', sparse su 21 file. Difficile avere una vista d'insieme di quali icone si usano, rischio di importazioni ridondanti o incoerenti per lo stesso concetto (es. icone di chiusura diverse in punti diversi).",
      "decision": "Un solo punto di importazione da 'lucide-react': packages/ui/src/utils/icons.ts. Esporta ogni icona per nome (re-export diretto) più un oggetto ICONS (nome→componente) usato dal catalogo visivo in DESIGN > Icone. Tutti i componenti importano le icone da qui — relativo ('../../utils/icons' o simile) se interni a @edg/ui, da '@edg/ui' se esterni (apps/*, packages/auth) — mai più direttamente da 'lucide-react'. In più, packages/ui/src/utils/index.ts espone iconMap: chiave semantica → icona (es. close: X, refresh: RefreshCw), pescando anch'esso da icons.ts. Regola di scelta: iconMap per le icone che ricorrono con lo stesso significato in punti distanti dell'app (navigazione, azioni standard come chiudi/aggiorna/modifica/elimina); import diretto da icons.ts per le icone che sono cromo strutturale di un singolo componente (frecce di ordinamento di una tabella, chevron di una select, icona calendario di un date-picker) — lì un nome semantico dedicato aggiungerebbe solo indirezione senza un concetto realmente condiviso da nominare. Per aggiungere una nuova icona: importarla in icons.ts una volta sola; se ricorre con lo stesso significato in più punti lontani, aggiungerla anche a iconMap.",
      "rationale": "Un catalogo unico rende visibile a colpo d'occhio quali icone esistono nel progetto (pagina DESIGN > Icone) ed evita import diretti sparsi. iconMap-per-tutto è stato scartato: molte icone sono cromo di un singolo componente senza un vero concetto riusabile altrove, e forzarle tutte in iconMap avrebbe richiesto nomi semantici artificiali (es. \"tableSortAsc\") senza benefici reali, oltre a un'indirezione in più nel JSX. Il criterio ibrido applica iconMap solo dove risolve un problema reale già osservato nel codice (X usato come \"chiudi\" in 4 componenti distanti, RefreshCw come \"aggiorna\" in 2), mantenendo import diretti semplici ed ergonomici ovunque il significato è locale al componente.",
      "status": "accepted"
    },
    {
      "id": "ADR016",
      "date": "2026-09-07",
      "title": "Elimina-se-libero, altrimenti-disattiva: basato sui vincoli FK del DB, non su un registro applicativo",
      "context": "Le tabelle di gestione (reparti, operatori, anagrafiche, e le prossime che seguiranno lo stesso pattern CRUD) devono poter essere eliminate davvero quando un record non è mai stato usato altrove, ma solo disattivate quando lo è (es. un reparto già assegnato a un operatore). Serviva un modo per sapere, in ogni istante, se un record è \"in uso\" senza dover mantenere a mano un elenco di dove ogni tabella viene referenziata — elenco che andrebbe aggiornato ad ogni nuova relazione futura e sarebbe facile dimenticare di aggiornare.",
      "decision": "crudFactory.remove() (system-service) tenta sempre prima l'eliminazione vera (record.destroy()). Se Postgres la rifiuta per un vincolo FK (SequelizeForeignKeyConstraintError — le FK verso queste tabelle sono già ON DELETE RESTRICT per ADR014), il backend ripiega automaticamente sulla disattivazione (isActive=false) invece di fallire, e lo comunica nel campo `message` della risposta (\"eliminato definitivamente\" vs \"disattivato: è referenziato altrove\"). Il frontend (useEntityCrud) mostra quel messaggio così com'è nel toast, senza assumere quale esito ci sia stato. Il popup di conferma resta volutamente generico (\"Eliminare X?\", mai \"Eliminare/disattivare\"): l'utente conferma l'intento di rimuovere, l'esito preciso si vede dopo, nel messaggio di risposta.",
      "rationale": "Il DB conosce già, tramite i propri vincoli FK, se un record è referenziato: usarlo come unica fonte di verità evita di duplicare quella conoscenza in codice applicativo (niente elenco \"tabella X è referenziata da Y, Z...\" da mantenere a mano) e la regola resta automaticamente corretta anche per relazioni future non ancora scritte oggi. Alternativa scartata: un controllo esplicito lato applicazione (query di conteggio sulle tabelle che referenziano) — più codice, va aggiornato ad ogni nuova relazione, rischio concreto di disallineamento dal reale schema del DB.",
      "status": "accepted"
    },
    {
      "id": "ADR017",
      "date": "2026-09-08",
      "title": "ADR017: Messaggi di validazione in italiano centralizzati + maxLength frontend a specchio",
      "context": "Il campo \"Provincia\" di Anagrafica ha uno schema Joi con max(4) (sigla, es. \"TE\"), ma l'utente ha inserito \"Teramo\" (nome esteso) ed è stato respinto con l'errore generico \"Dati non validi\". Indagando è emerso un problema più ampio: (1) solo alcuni campi degli schemi Joi (system-service) avevano `.messages()` italiani scritti a mano campo per campo; i campi senza override mostravano il messaggio Joi di default in inglese e tecnico (es. \"provincia length must be less than or equal to 4 characters long\"), e questo era già successo prima con L028 senza che il pattern fosse sistemico; (2) apiFetch scartava il dettaglio errors[] della risposta 400, mostrando sempre e solo il messaggio generico \"Dati non validi\" senza dire quale campo; (3) nessun vincolo di lunghezza lato frontend impediva di digitare oltre il limite prima del submit, quindi l'errore si scopriva solo dopo un round-trip al backend.",
      "decision": "Tre interventi complementari, tutti applicati: (A) middleware/validate.ts (system-service) ora passa un dizionario ITALIAN_MESSAGES globale a schema.validate() tramite l'opzione `messages`, con un messaggio italiano per ogni codice di errore Joi comune (string.max, string.email, any.required, number.positive, ecc.), usando i placeholder {{#label}} e {{#limit}}; i `.messages()` già definiti sul singolo campo restano prioritari (Joi li applica prima) per i casi che richiedono un testo più specifico del template generico. (B) Ogni campo Joi degli schemi (reparto, operatore, anagrafica) ha ora `.label('Nome in italiano')` così {{#label}} nei messaggi mostra un nome leggibile invece della chiave camelCase grezza (es. \"Sigla provincia\" invece di \"provincia\"). (C) apiFetch.ts (packages/auth, punto centrale usato da tutti i moduli) ora include nel messaggio d'errore lanciato anche il dettaglio `errors[]` della risposta (\"campo: messaggio\"), invece di scartarlo. (D) I campi Input del frontend (AnagraficaFormModal, OperatoreFormModal, RepartiTab) hanno ora `maxLength` allineato al vincolo Joi corrispondente (mirror manuale, non condiviso automaticamente — frontend e backend sono due repository separati), più helperText dove utile (es. \"Es. TE\" su Sigla provincia) e normalizzazione a maiuscolo per la sigla provincia.",
      "rationale": "Centralizzare i messaggi in un unico punto (invece di `.messages()` sparsi campo per campo) garantisce che OGNI campo, presente e futuro, in QUALSIASI schema del servizio, abbia sempre un messaggio in italiano comprensibile senza bisogno di ricordarsene ad ogni nuovo schema — elimina la classe di bug per cui alcuni campi restano \"dimenticati\" col default inglese di Joi. Il mirror manuale maxLength lato frontend è stato scelto come livello \"leggero\" rispetto a una condivisione automatica dello schema di validazione tra i due repository (edg-system/edg-docker sono separati, senza pacchetto condiviso oggi): un investimento strutturale in tal senso è stato esplicitamente rimandato a quando il prodotto multi-tenant SaaS prenderà forma, perché richiederebbe una vera infrastruttura di contratti condivisi tra repo.",
      "status": "accepted"
    },
    {
      "id": "ADR018",
      "date": "2026-09-08",
      "title": "ADR018: Convenzioni scheda dettaglio (click sul nome) ed esportazione PDF per gli elenchi",
      "context": "Nella tabella Anagrafiche la lista mostra solo 5 dei 14 campi del record (Ragione sociale, Tipo, Città, Referente, Stato) — Partita IVA, Codice fiscale, Indirizzo, CAP, Provincia, Telefono, Email, Note restano invisibili se non riaprendo il form di modifica, che però è pensato per modificare, non solo consultare. Contestualmente è stata richiesta anche l'esportazione PDF di ogni elenco della piattaforma, funzionalità finora assente ovunque.",
      "decision": "Due convenzioni condivise, entrambe implementate in @edg/ui per essere riusabili da ogni modulo futuro, non solo BASE: (1) Scheda dettaglio: nuovo componente `DetailModal` (packages/ui/src/components/ui/detail-modal), read-only, sezioni di coppie etichetta/valore su griglia — stessa estetica del Modal di creazione/modifica ma senza input. Si apre cliccando sulla colonna \"nome\" (in genere la prima) di una riga, sfruttando il meccanismo `clickable`/`onCellClick` già esistente in Table (nessuna modifica a Table necessaria). Applicata finora solo ad Anagrafiche (l'unico caso reale: la lista non mostra tutti i campi); Operatori e Reparti ne restano privi perché le loro tabelle già mostrano tutti i campi del record — il criterio è esplicitamente \"solo quando l'elenco non mostra già tutto\". Il DetailModal include anche un pulsante \"Modifica\" che passa direttamente al form. (2) Esportazione PDF: nuova utility `exportTableToPdf` (packages/ui/src/utils/exportPdf.ts, jsPDF + jspdf-autotable, generazione lato browser) e componente `ExportPdfAction` (variant 'outline', icona download, accanto a CreateAction). Applicata a tutte e tre le tabelle BASE (Anagrafiche, Operatori, Reparti). Il PDF riporta titolo, sottotitolo con i filtri attivi correnti, tabella con i colori del brand EDG (violet #7c3aed, da --action-primary), e piè di pagina con data/ora di generazione, conteggio record e numero pagina.",
      "rationale": "Entrambe le convenzioni sono state costruite come componenti/utility condivisi in @edg/ui (non ad hoc nelle pagine BASE) perché sono pattern trasversali a qualunque elenco della piattaforma, non specifici di questo modulo — coerente con l'approccio \"divide et impera\": ogni pagina futura le riusa passando solo i propri dati/colonne. Per il dettaglio si è scelta una modal (non una pagina con URL proprio) per coerenza con create/modifica già esistenti e per il minor sforzo, riservando una pagina dedicata a un'eventuale crescita futura del dettaglio (tab, allegati, storico). Per il PDF si è scelta la generazione lato browser (jsPDF) invece di un servizio di generazione documenti lato backend, più veloce da realizzare e senza nuova infrastruttura; un documento aziendale più curato (intestazione con logo, layout da vero documento) resta un'opzione da valutare più avanti se servirà per uso esterno/archiviazione formale.",
      "status": "accepted"
    },
    {
      "id": "ADR019",
      "date": "2026-09-08",
      "title": "ADR019: Helper isRoot in useAuth() per nascondere dettagli tecnici del dato",
      "context": "La scheda dettaglio (DetailModal, ADR018) doveva guadagnare una sezione con i dettagli tecnici del record (id, uuid, chiavi esterne come idTenant) visibile solo all'account root, mai a un utente normale anche se amministratore del proprio tenant. Non esisteva finora nel frontend alcun modo per verificare se l'account connesso è l'account root.",
      "decision": "Aggiunto `isRoot` (boolean) al return di `useAuth()` in packages/auth/src/hooks/useAuth.ts, calcolato come `account?.roleName === 'root'` — lo stesso `roleName` già presente in AuthAccount e popolato dal login (auth-service, AuthService.ts, da `account.role?.name`). Il ruolo 'root' è quello creato da root-account.seed.ts, l'unico con modulo wildcard '*'. Usato subito in AnagrafichePage.tsx per condizionare l'ultima sezione del DetailModal (ID, UUID, ID Tenant).",
      "rationale": "Stesso pattern già esistente per `isAccountType` in useAuth: un helper derivato, centralizzato in un unico punto, invece di ripetere `account?.roleName === 'root'` in ogni pagina che ne avrà bisogno in futuro (qualsiasi DetailModal di qualsiasi entità vorrà presumibilmente la stessa sezione \"dati tecnici solo root\"). Non è un controllo di sicurezza reale (id/uuid non sono segreti, il controllo di sicurezza vero resta lato backend/gateway) — è solo un filtro UX per non esporre rumore tecnico a chi non ne ha bisogno.",
      "status": "accepted"
    },
    {
      "id": "ADR020",
      "date": "2026-09-08",
      "title": "ADR020: Azione \"Dati tecnici\" cablata da Table stessa, per entità senza DetailModal",
      "context": "L'utente root vuole poter vedere sempre tutti i campi grezzi di un record (id, uuid, chiavi esterne) anche per le entità che non hanno un DetailModal curato (Reparti, Operatori) — a differenza di Anagrafiche, dove questo bisogno è già coperto dalla sezione \"Dati tecnici\" root-only dentro il DetailModal esistente (ADR018/019). Proposta iniziale dell'utente: una voce di menu azioni \"Dati tecnici\" solo per root, che apre una modal molto semplice (una colonna, testo piccolo, spaziatura minima, chiave/valore, solo X per chiudere).",
      "decision": "Realizzato esattamente come proposto dall'utente, ma con un'implementazione più radicale sul piano della riutilizzabilità: invece di un quickAction con callback (come edit/delete, che richiedono stato e logica nella pagina), l'azione \"Dati tecnici\" è interamente auto-contenuta in Table.tsx. Nuovo componente `TechnicalDetailsModal` (packages/ui/src/components/ui/technical-details-modal): dump grezzo Object.entries(record) → colonna singola, font-mono text-xs, spaziatura minima, nessun footer (solo la X di Modal). Table.tsx possiede da sola lo stato (`technicalDetailsItem`) e il rendering del modal; alla pagina basta passare `rowActions.quickActions.technicalDetails = { enabled: isRoot }` — zero stato, zero callback, zero definizione di campi da mostrare (a differenza del DetailModal curato, che richiede una funzione `detailSections` scritta a mano per ogni entità). Cablato su RepartiTab e OperatoriPage; non aggiunto ad AnagrafichePage perché lì il bisogno è già coperto dalla sezione tecnica dentro DetailModal (evita ridondanza di due modi diversi per la stessa cosa sulla stessa entità).",
      "rationale": "A differenza di edit/delete, \"Dati tecnici\" non ha alcuna logica specifica dell'entità: è sempre lo stesso Object.entries() del record già presente nella riga della tabella. Non c'era quindi motivo di richiedere a ogni pagina di gestire stato e modal come per edit/delete: centralizzando tutto in Table si ottiene il minimo sforzo possibile per ogni pagina futura (una riga: `technicalDetails: { enabled: isRoot }`) e una sola implementazione da mantenere. Table resta comunque disaccoppiata dal concetto di permessi/ruoli: riceve solo un booleano `enabled`, deciso dalla pagina tramite l'helper `isRoot` di useAuth (ADR019) — Table non importa né conosce @edg/auth.",
      "status": "accepted"
    },
    {
      "id": "ADR021",
      "date": "2026-09-10",
      "title": "ADR021: \"Dati tecnici\" reso automatico su ogni Table (TableCapabilitiesProvider) — supera il meccanismo di ADR020",
      "context": "L'utente ha chiarito un principio più ampio di quello coperto da ADR020: ogni Table rappresenta sempre righe di una tabella o di una query reale (mai un elenco statico), quindi l'azione \"Dati tecnici\" per root deve esistere SEMPRE, su ogni Table dell'applicazione, senza che ciascuna pagina debba ricordarsi di dichiararla — a differenza del meccanismo di ADR020, che richiedeva a ogni pagina di passare esplicitamente `rowActions.quickActions.technicalDetails = { enabled: isRoot }`.",
      "decision": "Table.tsx non riceve più `enabled` dalla pagina: legge `isRoot` da un nuovo React Context generico, `TableCapabilitiesContext` (packages/ui/src/components/data/table/TableCapabilities.tsx), tramite l'hook `useTableCapabilities()`. Il Context è popolato una sola volta nella root dell'app (pro-frontend/src/App.tsx, componente `AppTableCapabilities`) leggendo `isRoot` da `useAuth()`. La colonna azioni ora compare anche su Table senza alcun `rowActions` configurato, purché l'utente sia root (`showActionsColumn = Boolean(rowActions?.enabled) || isRoot`); gestita anche la modalità `mode: 'buttons'`, che prima non mostrava mai azioni extra oltre edit/delete — ora, se root, aggiunge un piccolo ActionMenu con la sola voce \"Dati tecnici\". Rimossa l'opzione `quickActions.technicalDetails` dal tipo `TableRowActions`: non è più qualcosa che una pagina dichiara. Le tre pagine che la usavano (AnagrafichePage, RepartiTab, OperatoriPage) sono state ripulite dalla riga `technicalDetails: { enabled: isRoot }` e, dove `isRoot` non serviva più ad altro, anche dall'import di `useAuth`.",
      "rationale": "packages/ui non deve dipendere da @edg/auth: è vero il contrario (@edg/auth usa componenti di @edg/ui nelle sue pagine di login/profilo), quindi far chiamare `useAuth()` direttamente da Table.tsx avrebbe creato una dipendenza circolare fra i due package. Il Context risolve il problema restando sul lato \"generico\": ui definisce solo la forma del dato (`{ isRoot: boolean }`), è l'app a popolarlo con la propria logica di autenticazione in un unico punto. Risultato: la garanzia \"dati tecnici sempre per root\" vale ora per definizione su ogni Table, presente e futura, senza possibilità di dimenticarla pagina per pagina — a costo di richiedere che ogni nuova app che usa @edg/ui istanzi il Provider una volta nella propria root (altrimenti il default è isRoot: false, fail-safe: mai esposto per errore).",
      "status": "accepted"
    },
    {
      "id": "ADR022",
      "date": "2026-09-10",
      "title": "ADR022: Isolamento multi-tenant lato server su system-service, con eccezione per accountType 'operatore'",
      "context": "Le route CRUD generate da crudFactory (usate anche da Anagrafiche, l'unica entità system-service con concetto di tenant) non applicavano alcun filtro di tenant lato server: GET /anagrafiche e GET /anagrafiche/:id non scopavano per tenantId a meno che il client non lo passasse esplicitamente in query, e POST /anagrafiche accettava idTenant direttamente dal body. L'utente ha confermato di voler forzare il tenant lato server, ma con un vincolo di business preciso: pro-frontend è accessibile SOLO da account di tipo 'operatore' (mai da partner/cliente/agente), e gli operatori devono poter agire su tutti i tenant per definizione — RBAC (ruoli/permessi) governa cosa un operatore può fare, non l'isolamento per tenant, che riguarda solo gli altri tre accountType.",
      "decision": "(1) GatewayUser (system-service/src/middleware/auth.ts) ha ora un campo tenantId: number | null, popolato da parsed.tenantId in requireAuth — il gateway lo inviava già in x-user-data da ADR009 (gatewayHeaders.js), mancava solo la lettura lato system-service: nessuna modifica a gateway o auth-service è stata necessaria. (2) crudFactory (system-service/src/utils/crudFactory.ts) ha una nuova opzione opt-in tenantScoped (+ tenantField, default 'idTenant'). Quando attiva, per ogni account con role !== 'operatore': list forza where.idTenant = user.tenantId sovrascrivendo qualsiasi valore client; getById/update/remove/toggleActive rispondono 404 (non 403, per non rivelare che il record esiste su un altro tenant) se il record appartiene a un tenant diverso; create forza idTenant nel body al tenant dell'utente, ignorando quello del client. Per role === 'operatore' nessuna di queste forzature scatta. (3) anagraficheRoutes.ts ha tenantScoped: true; Reparti e Operatori (le altre due route su crudFactory) restano invariate. Codice verificato solo con tsc --noEmit: NON ancora distribuito, perché lo stage production del Dockerfile di system-service contiene solo dist/ compilato, quindi serve un vero rebuild dell'immagine (docker compose build system-service && docker compose up -d system-service), non un semplice restart.",
      "rationale": "L'opzione opt-in su crudFactory (anziché una modifica generalizzata) evita di introdurre il concetto di tenant su entità che non ce l'hanno (Reparti, Operatori). Il 404 anziché 403 su cross-tenant evita di confermare l'esistenza di un record altrui. L'esclusione per accountType, verificata tramite req.user.role (che è già l'accountType inoltrato dal gateway), rispetta esattamente la regola di business data dall'utente: nessun account diverso da 'operatore' accede mai a pro-frontend, quindi l'isolamento per tenant è irrilevante lì per costruzione — è pensato per quando esisteranno account partner/cliente/agente su altri frontend. Test pratico di isolamento cross-tenant rimandato: non esiste ancora un secondo tenant reale nel DB (solo il tenant di sistema 'edg'), quindi per ora ci si limita a una verifica di non-regressione dopo il deploy.",
      "status": "accepted"
    }
  ]
}