# =============================================================================
# EDG PLATFORM - CONFIGURAZIONE AMBIENTE
# =============================================================================
# Copia questo file in .env e configura i valori

# =============================================================================
# DATABASE MYSQL
# =============================================================================
MYSQL_ROOT_PASSWORD=your-root-password-change-me
MYSQL_DATABASE=edg_auth
MYSQL_USER=edg_user
MYSQL_PASSWORD=your-mysql-password-change-me

# =============================================================================
# DATABASE MONGODB (Logger)
# =============================================================================
MONGO_LOG_USER=logger
MONGO_LOG_PASSWORD=your-mongo-password-change-me
MONGO_LOG_DATABASE=edglogger

# =============================================================================
# JWT & SECURITY
# =============================================================================
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
GATEWAY_SECRET=your-gateway-secret-key-change-in-production-min-32-chars

# =============================================================================
# EMAIL CONFIGURATION - DUAL EMAIL SYSTEM
# =============================================================================
# EDG Platform usa due caselle email separate:
# 1. NOREPLY: per comunicazioni automatiche (reset password, verifiche, ecc.)
# 2. ALERTS: per notifiche agli amministratori (sicurezza, errori critici, ecc.)
#
# Lascia vuoti per usare console log (default in sviluppo)
# Configura per abilitare invio email reale

# SMTP Server (es: smtp.gmail.com, smtp.office365.com)
EMAIL_HOST=

# SMTP Port (587 per TLS, 465 per SSL)
EMAIL_PORT=587

# Credenziali SMTP (stesso account per entrambe le caselle)
EMAIL_USER=
EMAIL_PASS=

# Email mittente per comunicazioni automatiche (reset password, verifiche, ecc.)
EMAIL_NOREPLY_FROM=noreply@edg.local

# Email mittente per alert agli amministratori
EMAIL_ALERTS_FROM=alerts@edg.local

# Email destinatario per alert di sicurezza (deve essere configurata!)
EMAIL_ALERTS_TO=

# Frontend URL per link reset password
FRONTEND_URL=http://localhost:5173

# =============================================================================
# ESEMPIO CONFIGURAZIONE GMAIL
# =============================================================================
# Per usare Gmail:
# 1. Abilita "App meno sicure" o usa "App Password" 
# 2. Configura:
#    EMAIL_HOST=smtp.gmail.com
#    EMAIL_PORT=587
#    EMAIL_USER=tuo-email@gmail.com
#    EMAIL_PASS=tua-password-o-app-password
#    EMAIL_FROM=tuo-email@gmail.com

# =============================================================================
# ESEMPIO CONFIGURAZIONE OUTLOOK/OFFICE365
# =============================================================================
# EMAIL_HOST=smtp.office365.com
# EMAIL_PORT=587
# EMAIL_USER=tuo-email@outlook.com
# EMAIL_PASS=tua-password
# EMAIL_FROM=tuo-email@outlook.com
